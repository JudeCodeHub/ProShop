import PagePlaceholder from "@/components/page-placeholder";

export const metadata = { title: "Categories" };

export default function CategoriesPage() {
  return (
    <PagePlaceholder
      title="Categories"
      description="Add, rename and delete product categories."
    />
  );
}
