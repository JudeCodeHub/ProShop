import PagePlaceholder from "@/components/page-placeholder";

export const metadata = { title: "Orders" };

export default function OrdersPage() {
  return (
    <PagePlaceholder
      title="Orders"
      description="Transaction history, filterable by date range, cashier and status."
    />
  );
}
