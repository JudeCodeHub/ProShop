import PagePlaceholder from "@/components/page-placeholder";

export const metadata = { title: "Held sales" };

export default function HeldSalesPage() {
  return (
    <PagePlaceholder
      title="Held sales"
      description="Sales you have parked, ready to resume at checkout."
    />
  );
}
