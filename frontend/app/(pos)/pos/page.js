import PagePlaceholder from "@/components/page-placeholder";

export const metadata = { title: "Checkout" };

export default function PosPage() {
  return (
    <PagePlaceholder
      title="Checkout"
      description="Search or scan products, build the cart, apply discounts, take payment and print the receipt."
    />
  );
}
